"""HTTP-роуты оркестратора. Этап 2: /chat, /agents, /tools."""

from __future__ import annotations

from fastapi import APIRouter

router = APIRouter()

# Этап 2:
# @router.post("/chat")
# async def chat(req: ChatRequest) -> ChatResponse: ...
