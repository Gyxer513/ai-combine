"""Sandbox-broker: единственный сервис с доступом к docker.sock.

Сжимает привилегию (порождение контейнеров) до крошечной, аудируемой поверхности
без LLM/инъекционной логики. Оркестратор обращается к нему по HTTP с минимальным
API {profile, command} — параметры hardening'а и allowlist клиент задать не может.
"""
